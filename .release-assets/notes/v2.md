# v2 — Runtime Six-Beam Blast / 运行时六束魔爆

## 中文
将 v1 的静态覆盖改为 Norbyte BG3 Script Extender 运行时修改 `EldritchBlast` LevelMap，并加入运行日志与验证。该路线成为后续版本的稳定技术基础。

**推荐使用最新 v8。**

## English
Replaced the v1 static override with a Norbyte BG3 Script Extender runtime mutation of the `EldritchBlast` LevelMap, with runtime logging and verification. This became the technical foundation for later versions.

**Use v8 for the current build.**
